# GitHub Personal Access Token
# REQUIRED to access private repositories
# Also recommended to avoid rate limits (60 req/hr without token, 5000 req/hr with token)
#
# Create one at: https://github.com/settings/tokens
#
# Required permissions:
# - repo (full control of private repositories) - REQUIRED for private repos
#   OR
# - public_repo (access public repositories only) - if you only want public repos
#
# Instructions:
# 1. Go to https://github.com/settings/tokens/new
# 2. Give it a descriptive name like "HavocLab Showcase"
# 3. Select expiration (recommend: No expiration for personal projects)
# 4. Check "repo" scope for private repos, or just "public_repo" for public only
# 5. Click "Generate token"
# 6. Copy the token and paste it below
#
GITHUB_TOKEN=your_github_token_here
